import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // TODO: Add image domains for Supabase storage if needed
  // images: {
  //   remotePatterns: [
  //     { protocol: "https", hostname: "*.supabase.co" },
  //   ],
  // },

  // TODO: Add redirects or rewrites if needed for auth flows
};

export default nextConfig;
